<Window x:Class="MonaJsonToMsp.Views.HelpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MonaJsonToMsp" 
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="CanResizeWithGrip" 
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        Title="helpWindow" Height="800" Width="600" FontSize="14"
        >
    <Window.Background>
        <SolidColorBrush Color="White" />
    </Window.Background>
    <materialDesign:DialogHost CloseOnClickAway="True" DialogTheme="Inherit" FontSize="14">
        <Grid>
            <Frame Name="frame" NavigationUIVisibility="Hidden"/>
            <Button x:Name="Close" HorizontalAlignment="Right" Margin="0,10,15,0" VerticalAlignment="Top" Height="25" Width="25" Click="Close_Click" Padding="0" Grid.Row="0">
                <materialDesign:PackIcon Kind="Close" HorizontalAlignment="Center" VerticalAlignment="Center" Width="30" Height="30"/>
            </Button>

        </Grid>
    </materialDesign:DialogHost>
</Window>
